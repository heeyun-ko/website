---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Outfit:wght@300;400;700&display=swap" rel="stylesheet">
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<div class="hero-content">
					<h1>Eindel's log</h1>
					<p class="hero-subtitle">Eindel is passionate to a lot of things in the world XD.</p>
					<a href="#cards" class="cta-button">Continue to Read</a>
				</div>
				<div class="hero-image">
					<img src="/header-bg.jpg" alt="Minimalistic art with rounded corners featuring green, pink, and blue shapes" />
					<div class="gradient-overlay"></div>
				</div>
			</section>

			<section class="content">
				<div class="content-wrapper">
					<h2>Get inspiration from her</h2>
					<p class="tagline">
						Designing your life That can<br/>
						<span class="highlight">be consistent.</span>
					</p>
					<div class="scroll-indicator">
						<div class="circle">
							<div class="dot"></div>
						</div>
					</div>
				</div>
			</section>

			<section class="cards" id="cards">
				<div class="card-grid">
					<div class="card">
						<img src="/images/workspace.jpg" alt="Workspace setup with multiple monitors" />
						<div class="card-content">
							<h3>Curious<br/>Eyes</h3>
							<p>She has a lot of potential in art, science, creating. In order to realize this ability, she explores new opportunities.</p>
						</div>
					</div>
					<div class="card">
						<img src="/images/selfie.jpg" alt="Eindel's casual selfie" />
						<div class="card-content">
							<h3>Aesthetic<br/>Lover</h3>
							<p>She loves all of the lovely creatures, objects, ideas. For her, Love means beautiful concept.</p>
						</div>
					</div>
					<div class="card">
						<img src="/images/mickey-white.jpg" alt="White Mickey Mouse shaped device" />
						<div class="card-content">
							<h3>Emotion<br/>Carer</h3>
							<p>She emphasizes the power of Sympathy. Always trying to catch up the flow of ppl's emotions.</p>
						</div>
					</div>
					<div class="card">
						<img src="/images/winter-style.jpg" alt="Winter fashion mirror selfie" />
						<div class="card-content">
							<h3>Systemic<br/>Thinker</h3>
							<p>She inherently likes to organize and optimize system. Her ambitions to achieve dreams is huge.</p>
						</div>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	:root {
		--primary-color: #8FA969;
		--text-color: #291F1E;
		--bg-color: #FFC9C5;
		--nav-bg: #FFFCF0;
		--white: #FFFFFF;
		--dark: #1E1E1E;
		--yellow: #FFD700;
		--pink: #FFB6C1;
		--light-green: #2E8B57;
		--dark-blue: #1E90FF;
	}

	body {
		margin: 0;
		padding: 0;
		font-family: 'Outfit', sans-serif;
		background: var(--bg-color);
	}

	.hero {
		position: relative;
		height: 1000px;
		overflow: hidden;
		margin-top: 0;
	}

	.hero-image {
		position: absolute;
		width: 100%;
		height: 100%;
		top: 0;
	}

	.hero-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center;
	}

	.gradient-overlay {
		position: absolute;
		width: 100%;
		height: 664.97px;
		bottom: 0;
		background: linear-gradient(180deg, rgba(30, 30, 30, 0) 0%, rgba(30, 30, 30, 0.70) 55%, #1E1E1E 100%);
		opacity: 0.4;
	}

	.hero-content {
		position: absolute;
		left: 143.33px;
		bottom: 143.33px;
		z-index: 1;
		color: var(--white);
		padding-top: 80px;
	}

	.hero-content h1 {
		font-family: 'Playfair Display', serif;
		font-size: 83.61px;
		font-weight: 600;
		line-height: 100.33px;
		margin: 0;
	}

	.hero-subtitle {
		font-size: 28.67px;
		line-height: 34.40px;
		margin: 0;
	}

	.cta-button {
		display: inline-flex;
		padding: 9.56px 19.11px;
		background: var(--white);
		color: var(--dark);
		text-decoration: none;
		font-size: 16.72px;
		margin-top: 20px;
		transition: all 0.3s ease;
	}

	.cta-button:hover {
		background: rgba(255, 255, 255, 0.9);
		transform: translateY(-2px);
	}

	.content {
		padding: 120px 143.33px 100px;
		position: relative;
		background: #FFFCF0;
		margin: 0;
	}

	.content-wrapper {
		max-width: 836.11px;
		position: relative;
		z-index: 1;
		margin: 0 auto;
	}

	.content h2 {
		font-family: 'Playfair Display', serif;
		font-size: 80px;
		font-weight: 400;
		line-height: 80px;
		color: var(--text-color);
		margin: 0;
	}

	.tagline {
		font-size: 28.67px;
		line-height: 34.40px;
		color: var(--text-color);
		font-weight: 300;
		margin: 20px 0;
	}

	.tagline .highlight {
		color: #FFB6C1;
		font-weight: 700;
	}

	.cards {
		padding: 50px 0 100px;
		margin-bottom: 100px;
		display: flex;
		justify-content: center;
		align-items: center;
		margin-top: 0;
		position: relative;
		z-index: 2;
		background: #FFFCF0;
		width: 100%;
	}

	.card-grid {
		display: grid;
		grid-template-columns: repeat(4, minmax(300px, 400px));
		gap: 2rem;
		width: 100%;
		max-width: 1800px;
		margin: 0 auto;
		padding: 0 1.5rem;
		justify-content: center;
	}

	.card {
		width: 100%;
		background: var(--white);
		border-radius: 10px;
		overflow: hidden;
		box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
		transition: all 0.3s ease;
		cursor: pointer;
	}

	.card:hover {
		transform: translateY(-10px) scale(1.02);
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
	}

	.card img {
		width: 100%;
		height: 350px;
		object-fit: cover;
		display: block;
		margin: 0;
		transition: transform 0.3s ease;
	}

	.card:hover img {
		transform: scale(1.05);
	}

	.card-content {
		padding: 2.5rem;
		background: var(--white);
		transition: transform 0.3s ease;
	}

	.card:hover .card-content h3 {
		transform: translateY(-2px);
	}

	.card-content h3 {
		font-family: 'Playfair Display', serif;
		font-size: 32px;
		line-height: 1.2;
		margin: 0 0 1rem 0;
	}

	.card-content p {
		font-size: 20px;
		line-height: 1.5;
		margin: 0;
		color: var(--text-color);
	}

	.card:nth-child(1) .card-content h3 {
		color: var(--yellow);
	}

	.card:nth-child(2) .card-content h3 {
		color: var(--pink);
	}

	.card:nth-child(3) .card-content h3 {
		color: var(--light-green);
	}

	.card:nth-child(4) .card-content h3 {
		color: var(--dark-blue);
	}

	.card.alternate .card-content {
		background: var(--white);
	}

	.card.alternate .card-content h3 {
		color: var(--text-color);
	}

	@media (max-width: 768px) {
		.hero-content {
			left: 20px;
			right: 20px;
		}

		.hero-content h1 {
			font-size: 48px;
			line-height: 1.2;
		}

		.content {
			padding: 60px 20px 100px;
			margin: 0;
		}

		.content h2 {
			font-size: 48px;
			line-height: 1.2;
		}

		.cards {
			margin-top: 0;
			padding: 30px 20px 100px;
		}

		.card-grid {
			grid-template-columns: 1fr;
			max-width: 400px;
			padding: 0 1rem;
		}

		.card {
			max-width: 100%;
		}
	}

	html {
		scroll-behavior: smooth;
	}
</style>
